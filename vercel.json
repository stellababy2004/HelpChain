{
  "version": 2,
  "builds": [
    {
      "src": "run.py",
      "use": "@vercel/python",
      "config": {
        "runtime": "python3.12",
        "installCommand": "python -m pip install --upgrade pip && python -m pip install -r requirements.base.txt --no-cache-dir --prefer-binary && python -m pip install Jinja2 PyJWT --no-cache-dir --prefer-binary"
      }
    }
  ],
  "routes": [
    {
      "src": "(.*)",
      "dest": "run.py"
    }
  ],
  "env": {
    "PYTHONPATH": "."
  }
}
