{
  "version": 2,
  "builds": [
    {
      "src": "run.py",
      "use": "@vercel/python",
      "config": {
        "installCommand": "python -m pip install --upgrade pip && python -m pip install -r requirements.txt --target vendor --no-cache-dir --prefer-binary"
      }
    }
  ],
  "routes": [
    {
      "src": "(.*)",
      "dest": "run.py"
    }
  ],
  "env": {
    "PYTHONPATH": "."
  }
}
