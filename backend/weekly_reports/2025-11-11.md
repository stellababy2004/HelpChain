# Седмичен отчет — 2025-11-11

## Нови функции и подобрения

- Добавен WebSocket клиент за стабилни real-time връзки.
- Обновен service worker за офлайн кеширане.
- Разширена конфигурация за Postgres и VAPID ключове.

## Deployment

- Включване на nginx snippet за статични assets:
  ```nginx
  include /path/to/your/repo/deploy/nginx_static_cache.conf;
  ```

## Troubleshooting

- Unregister на service worker и hard refresh при проблеми със статични assets:
  - DevTools → Application → Service Workers → Unregister, после Ctrl+Shift+R
  - Или в Console:
    ```js
    navigator.serviceWorker
      .getRegistrations()
      .then((rs) => rs.forEach((r) => r.unregister()));
    caches
      .keys()
      .then((keys) => Promise.all(keys.map((k) => caches.delete(k))));
    ```

## How-to

- Генериране на силен secret:
  ```pwsh
  python -c "import secrets; print(secrets.token_urlsafe(48))"
  ```
- Добави го в `.env`:
  ```env
  HELPCHAIN_SECRET_KEY=your_long_random_value_here
  ```

---

_Този отчет е автоматично структуриран за лесно проследяване на прогреса и deployment-а._

# Седмичен отчет — 11 ноември 2025

## Основни задачи

- Проверка и корекция на статични файлове (helpchain_qr.png)
- Интеграция на нови функции за заявки
- Проследяване на deployment

## Важни комити

- Добавяне на нови статични ресурси
- Корекции по Flask маршрути

## Напредък

- Статичните файлове са достъпни
- Новите заявки работят коректно

## Следващи стъпки

- Автоматизация на седмичните отчети
- Подобряване на документацията
